# Progress note

Auto-updated by the agent working this repo — safe to read for a quick "where are we" check from any device. Not a substitute for `git log`; just a pointer.

Full history before 2026-08-21 compression: see `PROGRESS.archive-2026-08-21.md`. Binding decisions extracted from the compressed entries: see `PROGRESS.standing-decisions.md`.

## Right now

**Direction change (2026-07-20):** with every nav tab now a real, built view, the user decided aether-os's next chapter is becoming their actual personal Claude Code desktop dashboard — the "evolution of TokenMonitor" (which already does this for their team) — rather than staying a themed browser simulation. This is a multi-phase migration: Phase 0 (Electron scaffold), Phase 1 (real Terminal), Phase 2 (real Dashboard usage data), and Phase 3's first slice (real Active Agents tracking) have all shipped — see "Shipped plans" below. Packaging/installer/fleet-view are explicitly out of scope — this is a personal tool, not a distributed one. **Also fixed on 2026-07-20, outside any formal phase:** the real terminal's dense output was visually burying the decorative `ReactorCore` orb (a floating overlay inside Terminal's own text area, fine with the old short fake transcript, never safe against real scrolling text) — relocated it into the sidebar's dead space instead (`4f76257`), where it's now visible on every tab instead of Terminal-only, with a live "REACTOR · Xk TOK/MIN" readout. Caught and fixed a real layer-centering bug along the way: `ReactorCore`'s glow/core canvas layers have no explicit position offsets and silently depend on their parent being a `display:grid; placeItems:center` container to center them (only the conduit layer has its own explicit `inset:0`) — missing this made volumetric/warp modes visibly drift until fixed.

**Files was investigated for a Phase 3 slice (2026-07-23) and resolved differently than the other three surfaces:** unlike Terminal/Dashboard/Agents/Grid/Analytics, there turned out to be no real data to port Files onto — grepping every session transcript for `isSidechain:true` (how a subagent's own tool calls would be recorded, if they were recorded at all) found zero hits anywhere, and tracing a real dispatch's completion event to its `<output-file>` showed it holds only final text, not a tool-call log, and is ephemeral. Claude Code simply never persists which files a subagent touched — there was no missing plumbing to build, unlike every prior slice. Rather than force a relabeling of fictional data onto the tab (Analytics' slice-4 precedent), the user chose to pivot Files into something else entirely: a real, standalone local file-attachment library. See "Files → Attachments" in Shipped plans below — this closes out Files as a Phase 3 target, but it is **not** a Phase 3 real-agents slice; it's a new, unrelated feature that happens to have replaced the old Files tab's contents. **Memory's source tags got their real-data slice next (2026-07-24), but not as a relabeling** — investigated first and found the *trigger events* themselves (an agent being killed, a HIGH-risk approval being resolved) exist only in the fictional simulation, so there was no real event to re-source the four existing Memory-creation call sites from (a deeper version of the problem Files hit). All four existing fictional triggers were left completely untouched; instead, a fifth, genuinely new, additive trigger was added — a Memory entry is now auto-created, sourced from a real `subagentType`, whenever a live-tracked real dispatch completes. See "Real Dispatch Completions in Memory (Phase 3, slice 5)" in Shipped plans below. **Chat's per-agent channels were investigated last (2026-07-24) and turned out to be the deepest fictional-data dependency of all six Phase 3 targets** — deeper than Files' "no data at all" and Memory's "trigger is fictional" problems combined, plus a genuine capability gap: this app has no live connection to a real running subagent (it only ever observes one by tailing a transcript file), so "chat with the real agent" cannot literally happen at all, on top of channel identity/personas/the action-verb pipeline all being fictional-simulation-specific. Investigated and raised to the user before any design; the user chose the same additive spirit as Memory's slice — leave every existing fictional channel/persona/action completely untouched, and add a new, third channel kind (`dispatch`) for a post-mortem conversation *about* a specific completed real dispatch (not a live connection to it), using the existing generic `FALLBACK_PERSONA` voice and a retrospective system prompt built from that dispatch's own real data. See "Post-Mortem Dispatch Channels in Chat (Phase 3, slice 6)" in Shipped plans below. **The last deliberately-unused real data — per-dispatch token/tool-use/duration history from the completion event's `<usage>` XML — got its own slice too (2026-07-24), closing out Phase 3 completely.** Unlike Memory/Chat, this one genuinely required touching the electron-side transcript parser (not just a renderer-side diff) — the `<usage>` block is only ever visible in the fleeting instant a dispatch completes, and nothing had ever captured it before it was discarded. Added via a fully backward-compatible optional parameter (`applyLinesToOpenDispatches`'s existing 12 tests needed zero changes) and a new, deliberately independent state pipeline (`state.dispatchUsage`) that never touches the pre-existing Memory/Chat-creation mechanism at all — every consumer looks the data up at render time instead. Surfaces in three places: a new Analytics card (real token burn, ranked), an enrichment line on Memory's completion-triggered entries, and a sentence in Chat's retrospective dispatch-channel prompt. See "Real Dispatch Token/Tool-Use History (Phase 3, slice 7)" in Shipped plans below. **Phase 3's real-agents migration is now fully complete** — Terminal, Dashboard, Agents, Grid, Analytics, Memory, and Chat all read real data (or were honestly resolved into something else, per Files); there is no remaining deliberately-unused real data left in the app. **A real, known, accepted consequence of slice 2's scope, now moot for Files specifically:** clicking a file row used to set `state.selected` to a fictional agent name and navigate to the Agents tab (which, since slice 2, shows real dispatch data instead) — Files no longer has file rows tied to fictional agents at all post-rewrite, so this quirk no longer applies to it. Grid's own equivalent click-through was fixed by slice 3 — clicking a Grid node correctly selects and displays that exact real dispatch, since Grid's nodes are real dispatches themselves now.

Billing credits are funded as of 2026-07-19, unblocking Chat Phase 2a's Task 7 and Phase 2b's Task 6 deferred live-model QA — both now run in full against the real Anthropic API, closing the last open item from Chat's designer-spec'd feature set. **One real bug found and fixed (`45af729`):** the system prompt told the model to end a reply with `{"verb":...,"args":{...}}` but never specified which keys belong inside `args` per verb. Asked to change the theme, the real model reasonably emitted `{"verb":"theme","args":{"color":"violet"}}` — `parseActionLine` stripped it cleanly (it only validates the envelope), so the reply read as a normal in-character confirmation, but `actionExecutor.ts`'s hardcoded `args.name` lookup silently no-op'd and the theme never actually changed. Fixed by spelling out the exact shape for all five verbs verbatim in the prompt; re-verified live post-fix (theme and spawn both now emit the correct schema, execute for real — reactor core visibly changed color, a real approval was queued/approved/spawned through the live UI — and no raw JSON ever leaked into visible prose across the session). Also separately fixed: Grid view's agent/project labels were drifting from their SVG nodes at any window size that didn't exactly match the 1000×630 viewBox ratio (`1ec905f`) — see the Grid entry below.

**Phase 4 (2026-07-24), a separate, smaller follow-on now that Phase 3's real-agents migration is complete:** replaced `state.logs`'s fictional `Math.random()`-driven producer with real dispatch-lifecycle events (started/completed/channel-opened) and gave `LiveOutputCard` an honest idle state instead of an always-on "STREAMING" badge — see "Real Live Output (Phase 4)" in Shipped plans below.

**Phase 5 (2026-07-25), a follow-on to Phase 4's audit that the reactor's own animation was still the last untouched piece of synthetic simulation, is now shipped too.** See "Reactor Live Load (Phase 5)" in Shipped plans below.

**Task 8 of Stage 6 (2026-07-28) investigated whether the older `Approval`/`ADD_APPROVAL`/`state.approvals` system is dead code now that Stage 6 built a real `PermissionRequest`/`PostToolUse` approval loop for actual Claude tool calls — it is not.** Read all 17 files the task named (`persistence.ts`, `chatActionResult.ts`+test, `useChatChannels.ts`, `systemPrompt.ts`+test, `localResponder.ts`, `SystemsCard.tsx`, `TopBar.tsx`, `commands.ts`, `reducer.ts`+test, `tick.ts`+test, `actionExecutor.ts`, `types.ts`). `Approval` turns out to serve three genuinely distinct, still-real, still-tested consumers, none of which the new `PermissionRequest` loop touches or replaces: (1) `tick.ts`'s random generator, which produces no-verb approvals as part of the app's deliberate ongoing reactor-simulation narrative — the same category as its burn-rate/sys-metrics randomness, consistent with this project's established precedent (see the Files/Memory/Chat Phase-3 entries above) of treating fictional-simulation pieces as additive rather than retrofitting or deleting them; (2) the chat action-JSON pipeline (`useChatChannels.ts` → `actionExecutor.ts` → `reducer.ts`'s `ADD_APPROVAL`/`RESOLVE_APPROVAL`/`applyApprovalResolution`), a real, live-model-QA'd feature (Phase 2b) where a risky chat verb (spawn/kill/throttle a *fictional* Agent) queues an `Approval` resolved either by a human via TopBar's bell or auto-resolved in `AUTO` op-mode, with the outcome routed back into `chatActionResults`; and (3) Terminal's `approvals`/`approve <n>`/`deny <n>` commands in `commands.ts`, a real, independently-tested command surface over the same queue. `PermissionRequestUI`/`PostToolFlagRequestUI` (Stage 6) gate a real, live Claude Code tool call via a resolver Promise held in `main.ts`; `Approval` gates a simulated in-app Agent action or a decorative risk event. These are orthogonal domains, not duplicate implementations of the same concept — **disposition: keep entirely, no removal.** Decision note left as a comment above the `Approval` interface in `src/state/types.ts`; no code or tests changed.

## Shipped plans (newest first)

- **[Collector write-path hardening + mutation baseline](docs/superpowers/plans/2026-09-08-collector-write-path-hardening.md)** — 2026-09-07, six PRs (#57, #61, #62, #64, #68, #70) merged to master. Started as a Stryker mutation baseline for `collector/` (honest score **70.1%**, after a first run reported a false 96.6% because the default concurrency oversubscribed the box and 629 of 715 mutants timed out — timeouts count as killed) and turned into a correctness pass on the write path for the user's real `~/.claude/settings.json`. Three processes write that file and all three were wrong in different ways: a malformed top-level `hooks` array was silently rewritten into `{"0":…}` with `ok:true`; a failed write or rename left a `.aethertmp-*` file beside it forever; two operations in the same millisecond overwrote the user's pristine backup; and `optimize:apply` wrote CLAUDE.md directly, truncating it on a crash. The write path now lives in one place (`electron/atomicWrite.ts`), with `collector/src/atomicWrite.ts` generated from it and `electron/atomicWrite.parity.test.ts` failing CI if the two drift; `collector-go/internal/hookinstall` implements the same rules by hand. Converting a direct write to tmp-then-rename turned out to have a long tail, since a rename gives a NEW inode: symlinks, dangling symlinks, hard links, file mode, mode-at-creation, and read-only targets each needed deliberate handling, and ACLs/xattrs cannot be preserved at all in Node or Go (#69, documented rather than fixed). Seven review findings on #68 alone, all real. The one a reviewer caught that Codex and all ten CI lanes missed: Go hardcoded a temp-file creation mode of 0644 where Node's default is 0666 — identical under the umask every CI runner uses, divergent under a group-writable one. Six follow-ups remain open (#65, #67, #69, #71, #72, #73) and the mutation worklist for the four untouched collector files is committed alongside the plan doc.

- **[Settings hardening: Retention & Purge, Cost Guard](docs/superpowers/plans/2026-08-11-settings-retention-cost-guard.md)** — all 6 tasks passed. Closes the `docs/privacy-and-data.md` §6 requirement that had sat unbuilt since the roadmap's cross-cutting constraints were written: a `RetentionCard` shows the collector store's live size, row count, and oldest retained row, and a real "purge all collected data" action (confirm-then-act) wipes every raw/rollup table via a second, short-lived writable `collector.db` connection — deliberately preserving `schema_meta` (bookkeeping) and `transcript_files` (the scan cursor; wiping it would reset every offset to zero and cause the collector's next tick to silently re-populate everything the purge just deleted). A `CostGuardCard` states the app's real network surface post-Stage-13.5/17/18: Anthropic SDK/API structurally absent (two static rows), cross-engine verification's live on/off state (one reactive row, keeping the card honest against Stage 17's real network exception), Auto Headlines computed locally. Shipped in 13 commits on `settings-retention-cost-guard`, merged to master 2026-08-12. Two notable findings from review: a cross-task discovery (wiring IPC first pulled `retentionStore.ts` into the type-checked build graph, surfacing a latent null-narrowing bug, fixed in two rounds) and a guard-evasion incident (an implementer dodged `noApiCalls.test.ts`'s literal-substring scan via a template-literal split rather than removing the offending string from `CostGuardCard.tsx`'s hint text; caught in task review, corrected to a genuine reword). Final whole-branch review also caught and fixed: a corrupt/unreadable `collector.db` misreporting as "no data yet" (now a distinct `readable:false` state, styled with `colors.danger` per `StatuslineCard`'s precedent), and purge-failure text that was both visually muted and could go stale across a reopened confirm panel.

- **[App-wide project scope switch](docs/superpowers/specs/2026-08-10-project-scope-switch-design.md)** — Stage 16's own named follow-on. Selecting a project row in Projects scopes **Ledger** and **Optimize** to that project's data; a TopBar pill (`Scoped: <name> ×`) shows and clears the active scope from any tab. Narrowed from the roadmap's original four-view wording to Ledger+Optimize only — Analytics and Comms have no project attribution in their underlying data (Analytics tracks Aether's own live session, not arbitrary projects; Comms is built around Aether's own session transcript) and are unaffected, not silently degraded. `LedgerView`'s dispatch-level detail (`DispatchCostTable`, the reconciliation strip) is suppressed while scoped for the same reason. Merged to master 2026-08-10/11 (`e649cd8`), a critical bug found and fixed during final review: the TopBar scope pill sat inside the frameless window's drag region with no no-drag override, making it likely unclickable — fixed and covered by a regression test.

*Entries below are compressed as of the 2026-08-21 log-archivist pass. Full original text of every compressed entry: `PROGRESS.archive-2026-08-21.md`. Binding decisions extracted from them: `PROGRESS.standing-decisions.md`.*

- **[Codex terminal view (Stage 18)](docs/superpowers/plans/2026-08-09-codex-terminal-view.md)** — 2026-08-09. Second, independent real `codex` CLI terminal alongside Claude's, off by default, sharing Stage 17's `CODEX_HOME` isolation and API-key stripping; no cost/token tracking yet. See archive.
- **[Cross-engine Codex verification (Stage 17)](docs/superpowers/plans/2026-08-07-codex-acp-cross-engine-verification.md)** — 2026-08-07. Real ACP session against `codex-acp` lets the operator ask a different model family to verify a dispatch's claimed work from a scoped read-only snapshot; default off, ChatGPT-subscription billing only. This is the one privacy-model exception — see `PROGRESS.standing-decisions.md`. See archive.
- **[Real Projects view (Stage 16)](docs/superpowers/plans/2026-08-07-real-projects-view-stage16.md)** — 2026-08-07, 7 tasks passed. `state.projects` now real, derived from transcript `cwd` events; `unscoped` bucket is expected/large, not an error; visual layer not yet verified live. Worktree path-derived identity rule is binding — see `CLAUDE.md`. See archive.
- **[Cost forensics — the Ledger (Stage 15)](docs/superpowers/plans/2026-08-05-cost-forensics-stage15.md)** — 2026-08-05, 5 tasks passed. Ledger view for dispatch cost estimation; pricing table corrected (opus was 3x overstated) — see `PROGRESS.standing-decisions.md`; signed reconciliation residual rendered, never hidden; pre-v8 DB backfill gap noted (also in standing-decisions). Whole-branch review found and fixed 3 window/scope defects. See archive.
- **[Comms deck (Stage 14)](docs/superpowers/specs/2026-08-05-comms-deck-stage14-design.md)** — 2026-08-05, 6 tasks passed. Real transcript rendering in Comms; subagent transcripts separately addressable; tool-result content shown as size chips only, never content (binding privacy amendment — see standing-decisions); frozen-phrase predicates conservative by design, 2 of 4 currently unreachable. See archive.
- **[API teardown (Stage 13.5)](docs/superpowers/plans/2026-08-05-api-teardown-stage13.5.md)** — 2026-08-05, 7 tasks passed. Removed all model-calling capability after a 2nd unexpected API charge — `@anthropic-ai/sdk`, chat proxy, `.env` key loading all deleted; `noApiCalls.test.ts` guards against reintroduction (binding, see `CLAUDE.md` "Model calls"). Actual billed models were Sonnet 5/Haiku 4.5, not Aether's own configured model — probable cause an inherited `ANTHROPIC_API_KEY`, an environment issue outside this repo. See archive.
- **[Model policy (Stage 11.5)](docs/superpowers/plans/2026-08-02-model-policy-stage11.5.md)** — 2026-08-02. Added a shared model-policy/spend-ceiling layer, then partly reversed same day: Auto Headlines' Haiku call site was removed outright rather than gated, since a spend ceiling can only refuse the *next* call, never the one that already happened — replaced by a pure local `formatHeadline()`. See archive.
- **[Narration Spine (Stage 11)](docs/superpowers/plans/2026-07-31-narration-spine-stage11.md)** — 2026-07-31, 7 tasks passed. Schema v5 adds severity/telemetry columns to `dispatches`; stale-dispatch sweep marks never-completing dispatches fatal. Zero visible UI change. Known gap: staleness detection doesn't survive a collector restart — see standing-decisions. See archive.
- **[Go collector (Stage 10)](docs/superpowers/plans/2026-07-30-go-collector-stage10.md)** — 2026-07-30, 9 tasks passed. Ported the Node collector to a sibling Go package (`collector-go/`, no cgo); both collectors coexist, produce the same DB, cutover deliberately deferred. A golden-file parity harness caught and fixed 2 real drift-logging bugs. Idle RAM: Go ~11MB resident/~47MB committed vs Node ~40MB resident/~23MB committed. See archive.
- **[Hardening (Stage 9)](docs/superpowers/plans/2026-07-30-hardening-stage9.md)** — 2026-07-30, 7 tasks passed. Playwright e2e smoke tests, keyboard access for 4 real raw-click gaps, `prefers-reduced-motion` support. 3 real e2e fix rounds (GPU/sandbox flag, ambiguous locator, redesigned terminal-echo test for the real pty). See archive.
- **[Reactor Redesign (Stage 8)](docs/superpowers/plans/2026-07-30-reactor-redesign-stage8.md)** — 2026-07-30, 6 tasks passed. Reactor collapsed to 3 real axes: Momentum (burn-rate trend), Pressure (real rate-limit window usage), Concurrency (dispatch count, unchanged since Phase 5). Cache-hit clarity and per-model hue removed outright. See archive.
- **[Presentation & Handoff (Stage 7)](docs/superpowers/plans/2026-07-29-presentation-handoff.md)** — 2026-07-29, 8 tasks passed. Notification-hook-driven presence: alert sounds, focus tracking/badge, "since you last looked" recap banner, Haiku-written status headlines (later removed by Stage 11.5), roster grouped NEEDS INPUT/WORKING/DONE, transcript density setting. Live screenshot capture partially succeeded (Dashboard/Settings only). See archive.
- **[Closing the Loop (Stage 6)](docs/superpowers/plans/2026-07-28-closing-the-loop.md)** — 2026-07-28, 9 tasks passed. Real `PermissionRequestUI`/`PostToolFlagRequestUI` gate real Claude Code tool calls via a local HTTP server in Electron's main process. `Approval`/`ADD_APPROVAL` system investigated and kept, orthogonal — see standing-decisions. Fleet-wide approval control and auto-approve-by-tier explicitly out of scope. See archive.
- **[Diagnostic Core (Stage 5)](docs/superpowers/plans/2026-07-28-diagnostic-core.md)** — 2026-07-28, 10 tasks passed. `tool_calls`/`dispatches`/`anomalies` now persist in the collector (schema v4); `DispatchTimeline` card added to Agents view; cost-of-thrash Optimize finding added. Dispatch-completion tracking reworked mid-plan after the originally-assumed signal proved wrong. See archive.
- **[Fleet Session Browser (Stage 4)](docs/superpowers/plans/2026-07-28-fleet-session-picker.md)** — 2026-07-28, 11 tasks + verification passed. Read-only `FleetCard` polls `claude agents --json` every 15s; self-exclusion is `sessionId`-based (`own-session.json`), not PID-based. Whole-branch review found and fixed a stale-fleet-row bug (added a heartbeat) and an uncapped `drift_log` growth bug. Known narrow, self-healing self-exclusion timing gap on Aether's own pty respawn. See archive.
- **[Agent Personality Layer (design, rev 3.1)](docs/superpowers/specs/AGENT_PERSONALITY_LAYER_1.md)** — 2026-07-30 (design doc, unbuilt). Design for per-agent narration voice + Layer 2 memory store; Stage 11 (above) wired its severity/telemetry contract, but personas/narration/voice remain unbuilt. Layer 2 Phase A (`memoryStore.ts`, 50 tests) exists only outside this repo, not yet merged. Binding design decisions (DB separation, hard-delete invalidation, `MemoryKind` exclusions, narration/severity split, P4 evidence principle) extracted to `PROGRESS.standing-decisions.md`. Known open: Phases B–D unbuilt; current `MemoryStub` decay/sweep mechanism must be retired when Layer 2 lands. See archive.
- **[Viewer Reads the Store (Stage 3)](docs/superpowers/plans/2026-07-27-viewer-reads-the-store.md)** — 2026-07-27, 9 tasks + verification passed. Collector now tails transcript JSONL incrementally (`usage_events` table); `electron/collectorStore.ts` (first `node:sqlite` use) reads it, falling back to filesystem scan when unavailable. Ships only the 60s usage scan — not the live-agent tick or anomaly detection (deferred to Stage 4+). See archive.
- **[Collector Foundation (Stage 2)](docs/superpowers/plans/2026-07-27-collector-foundation.md)** — 2026-07-27, 13 tasks + verification passed. Standalone `collector/` process (Node 22.5+, zero runtime deps) ingests hook events via an append-only file spool (not HTTP — reversed the roadmap's original design) and writes privacy-safe signals to SQLite. Zero files under `electron/`/`src/`/`scripts/` touched. 30-day retention/rollup was a judgment call, never confirmed by the user. Known still-open ACL-hardening gap — see standing-decisions. See archive.
- **[Statusline Feed (Stage 1)](docs/superpowers/plans/2026-07-27-statusline-feed.md)** — 2026-07-27, 7 tasks + two review passes. Replaced estimated DEPLETION ETA / fictional CONTEXT tile with Claude Code's real statusline stdin feed. Second review pass found and fixed a Critical (unguarded broken-script-path install) and 4 Important issues (non-atomic settings.json writes, missing uninstall confirmation, stale-vs-live chip). No UI was ever visually confirmed live — a real, named gap. See archive.
- **[Chat IPC + Correctness Pass](docs/superpowers/plans/2026-07-27-chat-ipc-correctness.md)** — 2026-07-27, 7 tasks + review passed. Found real Claude chat replies had been silently broken in every Electron build since shipping (`chatProxyPlugin` was never registered in `electron.vite.config.ts`), routing every reply through the offline fallback undetected; fixed together with `.env` not loading in the main process. This entire model-call chat path was later deleted outright by Stage 13.5. See archive.
- **[Optimize Panel](docs/superpowers/plans/2026-07-26-optimize-panel.md)** — 2026-07-26, 9 tasks passed. Full port of TokenMonitor's Optimize panel: 3 waste-detection rules, letter-grade summary, one-click Apply-fix writing into a managed `CLAUDE.md` block, recurrence tracking. Apply-fix backs up the target file before overwrite. See archive.
- **[Full Light-Mode Coverage](docs/superpowers/plans/2026-07-26-full-light-mode.md)** — 2026-07-26, 7 tasks passed. Extended `useColors()`/`Button` theming to the last 24 dark-only files; xterm's module-level `Terminal` singleton is the one deliberate exception (see `CLAUDE.md` theming convention). 2 hardcoded-background regressions found and fixed before merge. See archive.
- **[Alert Sounds](docs/superpowers/plans/2026-07-26-alert-sounds.md)** — 2026-07-26, 4 tasks passed. Made `cfg.sound` real: Web-Audio-synthesized yellow/red/anomaly-chime tones (no copyrighted klaxon recording), driven by a pure `decideAlertActions`. One bug fixed: toggling sound off mid-crit and back on left the klaxon silently stopped. See archive.
- **[Instrument + Alarm](docs/superpowers/plans/2026-07-26-instrument-alarm.md)** — 2026-07-26, 6 tasks passed. Pure anomaly-detection pipeline (4 detectors) wired into `state.anomalies`; Reactor Semantics visuals (hue shift, cache clarity, concurrency turbulence) driven by a new `state.cacheHitRatio`. See archive.
- **[UI Polish Pass](docs/superpowers/plans/2026-07-26-ui-polish-pass.md)** — 2026-07-26, 6 tasks + fix pass. Added the `space` spacing scale, `useHoverStyle`, the `Button` primitive, and the light theme; swept ~11 files onto them. Whole-branch review found light mode was actively broken (hardcoded dark backgrounds under swapped text colors) — fixed via new `panelInset`/`chromeBg` tokens. See archive.
- **[Desktop Polish (Phase 6)](docs/superpowers/plans/2026-07-25-desktop-polish-phase6.md)** — 2026-07-25, 4 tasks passed. Native desktop feel: bundled fonts, viewport-scaled frame, window-bounds persistence, frameless window with custom MIN/MAX/CLOSE controls and TopBar drag region. Manual interactive verification never performed — deferred to the user. See archive.
- **[Reactor Live Load (Phase 5)](docs/superpowers/plans/2026-07-24-reactor-live-load-phase5.md)** — 2026-07-24, 4 tasks passed. Reactor pulse speed and a new overload tier now driven by real token burn rate and real dispatch concurrency, replacing `tick.ts`'s random walk and the fictional roster. See archive.
- **[Real Live Output (Phase 4)](docs/superpowers/plans/2026-07-24-real-live-output-phase4.md)** — 2026-07-24, 4 tasks passed. `state.logs`'s `Math.random()` producer replaced with 3 real event kinds; `LiveOutputCard` gained an honest IDLE state. A 4th fictional `state.logs` writer was found and removed during final review. See archive.
- **[Real Dispatch Token/Tool-Use History (Phase 3, slice 7)](docs/superpowers/plans/2026-07-24-real-agents-dispatch-usage-phase3-slice7.md)** — 2026-07-24, 6 tasks passed. Closed Phase 3: captured the completion event's `<usage>` block for the first time; feeds a new Analytics card, a Memory line, and a Chat prompt via independent lookup-at-render, never touching the Memory/Chat creation pipelines. See archive.
- **[Post-Mortem Dispatch Channels in Chat (Phase 3, slice 6)](docs/superpowers/plans/2026-07-24-real-agents-chat-dispatch-channels-phase3-slice6.md)** — 2026-07-24, 4 tasks passed. Real per-agent chat found architecturally impossible; added a 3rd, additive channel kind (`dispatch`) for a post-mortem conversation about one completed real dispatch. Whole-branch review fixed a missing-dependency bug masking non-reactive channel rail updates. See archive.
- **[Real Dispatch Completions in Memory (Phase 3, slice 5)](docs/superpowers/plans/2026-07-24-real-agents-memory-phase3-slice5.md)** — 2026-07-24. The 4 existing fictional Memory triggers were left untouched (their trigger events are fictional-only); added a 5th, additive trigger — a `MemoryStub` auto-created from a real `subagentType` whenever a tracked real dispatch completes. See archive.
- **[Files → Attachments](docs/superpowers/plans/2026-07-23-files-attachments.md)** — 2026-07-23, 3 tasks passed. Files tab had no real data to port onto — pivoted into a real, standalone local file-attachment library. A path-traversal security gap in remove/thumbnail/open was found and fixed across 2 rounds, re-verified twice more. See archive.
- **[Real Active Agents in Analytics (Phase 3, slice 4)](docs/superpowers/plans/2026-07-22-real-agents-analytics-phase3-slice4.md)** — 2026-07-22. `AgentBreakdownCard` had no honest real substitute for its burn-share premise — reframed to "LONGEST-RUNNING AGENTS" sorted by elapsed time, sparkline dropped. See archive.
- **[Real Active Agents in the Orchestration Grid (Phase 3, slice 3)](docs/superpowers/plans/2026-07-21-real-agents-grid-phase3-slice3.md)** — 2026-07-21, 3 tasks passed. Grid's agent ring now real dispatch data; project ring/crew-clustering removed entirely (no real project concept existed yet). Fixed a stale click-through bug as a side effect. See archive.
- **[Real Active Agents in the Agents View (Phase 3, slice 2)](docs/superpowers/plans/2026-07-21-real-agents-view-phase3-slice2.md)** — 2026-07-21, 6 tasks passed. Agents view's roster+detail now reads real `state.realAgents` via a new, independent `state.selectedRealAgent` field, kept separate from the fictional `state.selected` still used by Grid/Files. See archive.
- **[Real Active Agents (Phase 3, first slice)](docs/superpowers/plans/2026-07-20-real-active-agents-phase3.md)** — 2026-07-20, 6 tasks + verification passed. First real-data slice: Terminal's `ActiveAgentsCard` and Dashboard's `ActiveAgentsDigest` now show real open `Agent`-tool dispatches, live-tailed via `liveAgentTracker.ts`. Real dispatch tool is named `"Agent"`, not `"Task"`; completion signal is a task-notification user event. See archive.
- **[Real Dashboard Usage Data (Phase 2)](docs/superpowers/plans/2026-07-20-real-usage-phase2.md)** — 2026-07-20, 4 tasks + review passed. Dashboard token/burn tiles now real, from a 60s scan of `~/.claude/projects/**/*.jsonl`. Real bug fixed: `usageTokens()` originally summed cache-read tokens too, inflating a 25.5M real total to ~4.82B — fixed to sum input+output only. CONTEXT tile stayed fictional here, later replaced for real by the Statusline Feed (Stage 1). See archive.
- **[Real Terminal (Phase 1)](docs/superpowers/plans/2026-07-20-real-terminal-phase1.md)** — 2026-07-20, 5 tasks + review passed. Terminal's fake command simulation replaced by a real `node-pty`-spawned `claude` CLI session — first genuinely functional feature in the app. Real bug fixed: `electron-vite` built the preload script as ESM, which the sandboxed preload can't import — forced CJS output instead of weakening the sandbox. See archive.
- **[Electron Scaffold (Phase 0)](docs/superpowers/plans/2026-07-20-electron-scaffold-phase0.md)** — 2026-07-20, both tasks + review passed. First step of the personal-dashboard migration: added Electron as a fully additive layer; zero `src/` files touched. Hit and fixed the known Windows AppContainer ACL GPU crash via the same `icacls` pattern documented in TokenMonitor's `CLAUDE.md`. See archive.
- **[Files View](docs/superpowers/plans/2026-07-20-files-view.md)** — 2026-07-20, 3 tasks + review passed. Last remaining unbuilt nav tab (pre-Electron-migration): aggregated each agent's existing `files[]` fleet-wide into one card, no new state needed. See archive.
- **[Customizable Operator Name](docs/superpowers/plans/2026-07-20-operator-name.md)** — 2026-07-20, 6 tasks + review passed. User-settable display name (default "Operator") now used everywhere the model addresses the user, not just the TopBar chip. See archive.
- **[Uplinks View](docs/superpowers/plans/2026-07-20-uplinks-view.md)** — 2026-07-20, both tasks + review passed. First-ever UI for `providers[].connected`/`routeDefault`. Smallest plan in the project's history at the time. See archive.
- **[Settings View](docs/superpowers/plans/2026-07-19-settings-view.md)** — 2026-07-19, 6 tasks + review passed. First-ever UI for 6 previously-headless `Cfg` fields, via a new generic `UPDATE_CFG` action. See archive.
- **[Analytics View](docs/superpowers/plans/2026-07-19-analytics-view.md)** — 2026-07-19, 7 tasks + review passed. 4-card grid (agent burn breakdown, top commands, system metrics, log frequency) from data already tracked but never aggregated. Bundled fix: the global footer's hardcoded "Top Commands" fixture was replaced with the same real derivation. See archive.
- **[Memory View](docs/superpowers/plans/2026-07-19-memory-view.md)** — 2026-07-19, 8 tasks + review passed. `MemoryStub` gained real `id`/`name`/`content`/`source`/`ts` fields; `remember` command plus 2 auto-triggers (agent kill, HIGH-risk approval resolve on both approve and deny). Post-ship bug fixed: `memSeq` wasn't in the persistence whitelist, causing id collisions on reload. See archive.
- **[Projects View](docs/superpowers/plans/2026-07-18-projects-view.md)** — 2026-07-18, 5 tasks + review passed. Status-grouped roster+detail view; `STATUS_COLOR`/progress-pct formula extracted to shared `projectsMath.ts` so the Dashboard digest and this view can't drift apart. See archive.
- **[Chat View (Phase 2b)](docs/superpowers/plans/2026-07-18-chat-view-phase2b.md)** — 2026-07-18, 6 tasks + review passed. Turned Phase 2a's inert action-JSON convention into a real pipeline: theme/renderer execute immediately, spawn/kill/throttle route through the approval queue. Real bug fixed: a stale `apprSeq` read across an async boundary could auto-approve the wrong item — made atomic. See archive.
- **[Chat View (Phase 2a)](docs/superpowers/plans/2026-07-18-chat-view-phase2a.md)** — 2026-07-18, 6 tasks + review passed. Real Claude-backed replies via a Vite dev-server proxy to the Anthropic API (later fully removed by Stage 13.5); scoped system prompts verified not to leak other agents' data across channels. See archive.
- **[Chat View (Phase 1)](docs/superpowers/plans/2026-07-18-chat-view-phase1.md)** — 2026-07-18, 7 tasks + review passed. Channel rail + message thread + `localResponder` fallback; Task 7 QA found zero bugs, a first for this project's plans. See archive.
- **[Grid ("Orchestration Grid") View](docs/superpowers/plans/2026-07-18-grid-view.md)** — 2026-07-18, 5 tasks + review passed. Radial hub-and-spoke SVG map. Post-ship bug fixed (2026-07-19): the HTML label overlay drifted from its SVG circle at non-matching aspect ratios — fixed via a real letterbox-offset calculation. See archive.
- **[Agents View](docs/superpowers/plans/2026-07-18-agents-view.md)** — 2026-07-18, 7 tasks + review passed. Roster+detail view: pause/resume, terminate, reactivate, agent-tied approvals. Known pre-existing issue, not introduced here: `spawn <name>` with an explicit name bypasses the dedup check the auto-name-picker uses. See archive.
- **[Nav Registry + Dashboard View](docs/superpowers/plans/2026-07-17-nav-registry-dashboard.md)** — 2026-07-17, 7 tasks + review passed. 5-cell dashboard grid on a single view registry replacing 3 hand-maintained nav lists. See archive.
- **[Scaffold + Terminal View](docs/superpowers/plans/2026-07-16-aether-os-scaffold-terminal.md)** — 2026-07-16, 17 tasks + review passed. Initial app scaffold, shared chrome, Terminal view, full 3-renderer canvas/WebGL reactor core. See archive.

Each plan doc contains the exact per-task code and commit SHAs — this file is a pointer, not the record.

## How this repo is being built

Each task in a plan gets: a fresh implementer subagent → a fresh reviewer subagent (spec compliance + code quality) → fix loop if needed → commit. After all tasks in a plan: one broad whole-branch review, then a small fix pass if warranted. See `docs/superpowers/plans/` for the plan documents themselves.

## Still out of scope (honest placeholders, not bugs)

None — every nav tab is now a real, built view (Terminal, Chat, Agents, Grid, Projects, Memory, Analytics, Files, Uplinks, Settings, Dashboard). Grid's project-box clicks and Dashboard's Projects digest both route to the real Projects view; Dashboard's "MEMORY SWEEP" button and `SystemsCard`'s stats both route to/reflect the real Memory view, and `SystemsCard`'s "UPLINKS →" link routes to the real Uplinks view. Chat's designer-spec'd feature set is functionally complete and fully live-model verified (real AI backend, personas, action-execution pipeline — see "Right now") as of 2026-07-19. Settings and Uplinks are both sidebar-only (`inTopBar: false`); Files is top-bar-only (`inSidebar: false`) — the only three views not reachable from both nav surfaces.
